# C++ extension

This directory is reserved for performance-oriented implementations after profiling identifies a genuine bottleneck in the Python reference implementation.

The Python implementation remains the numerical reference until a C++ version is validated against it.
